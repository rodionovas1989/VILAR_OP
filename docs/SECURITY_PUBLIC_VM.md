# Безопасность: публичная ВМ (пилот)

Контекст: пилот на Yandex Cloud, **https://vilar-prod.ru** (nginx + Let’s Encrypt), сервис `vilar-op`, SQLite на диске ВМ.

## Текущая оценка

### Что уже защищает данные

| Мера | Статус |
|------|--------|
| API без токена (кроме `/health`, login, logout) | есть |
| RBAC на коллекциях/документах | есть |
| Пароли: scrypt + timing-safe compare | есть |
| Единое сообщение «Неверный логин или пароль» | есть |
| CORS allowlist (`CORS_ORIGIN`) | есть |
| Секрет токена: `AUTH_SECRET` / файл `auth_secret` | есть |
| Подсказок Admin/пароля в UI | нет |
| Гостевой UI: только форма входа (после `feature/auth-gate-landing`) | да |
| Слабые демо-пользователи удалены (только Admin) | да (пилот) |
| Rate limit на `POST /api/auth/login` (по IP и логину; до HTTPS ок) | да |
| HTTPS (nginx + Let’s Encrypt) на `vilar-prod.ru` | да (пилот) |

Данные справочников и документов **не отдаются** без Bearer-токена. Это не «открытая БД в браузере».

### Реальные риски при публикации IP в интернет

| Риск | Почему важно | Критичность |
|------|----------------|-------------|
| **HTTP без TLS** | ~~открытый текст~~ → HTTPS на vilar-prod.ru | **снято** |
| **Порт 3001 наружу** | ~~публичный Node~~ → только localhost за nginx | **снято** |
| **Нет rate limit на login** | Перебор паролей | **снято** (лимит на login) |
| **Токен в localStorage / sessionStorage** | XSS → кража сессии; нет httpOnly cookie | средняя |
| **Нет Helmet / security headers** | Слабее защита от XSS/clickjacking | средняя |
| **SQLite + бэкапы на той же ВМ** | Компрометация ОС = компрометация данных и истории | высокая |
| **SSH с паролем/слабым ключом** | Обход приложения целиком | высокая |
| **Раскрытие факта пилота** | Health `{"ok":true}`, заголовок после входа, структура API | низкая (информация) |
| **Admin с сильным паролем** | Если пароль слабый или утёк — полный доступ | зависит от пароля |

**Вывод:** для закрытого LAN / VPN пилот приемлем. Для **открытого интернета по HTTP** — осознанный риск пилота. Данные за auth; дальше обязательны HTTPS и защита login от перебора.

---

## План мероприятий, если заказчик остаётся на ВМ

Приоритет при **публичном** доступе: HTTPS + anti-bruteforce + бэкапы вне ВМ. Белый список IP — запасной вариант (офис/VPN), не основной путь.

### Сделано
- [x] Обезличенный экран входа (без бренда до авторизации)
- [x] Убраны слабые пользователи (оставлен Admin с сильным паролем)
- [x] Auth + RBAC на API, scrypt, CORS
- [x] Rate limit на `POST /api/auth/login` (по IP и логину; до HTTPS ок)
- [x] HTTPS: `https://vilar-prod.ru` (nginx + Let’s Encrypt; порт 3001 снаружи закрыт)

### Этап A — следующее по порядку (публичный пилот)

1. ~~Белый список IP~~ → **отложено как идея**
2. ~~HTTPS~~ → **сделано** (`docs/HTTPS_SETUP.md`, скрипт `local/setup-https.sh`)
3. ~~Rate limit на login~~ → **сделано**
4. **SSH только по ключу**, парольный вход выключен; по возможности сузить 22.
5. **Бэкап sqlite вне ВМ** + проверка восстановления.

### Этап B — усиление приложения (1–2 недели)

6. **Helmet** / security headers (CSP, frame deny, HSTS после HTTPS).
7. **Сессии:** httpOnly Secure cookie вместо долгого токена в `localStorage`.
8. **Журнал входа** (успех/неуспех, IP, login).
9. Не светить лишнее в `/api/health`.

### Этап C — эксплуатация ВМ

10. Патчи ОС, мониторинг, регламент деплоя с `main`, план инцидента (смена `AUTH_SECRET`, паролей, откат БД).

### Этап D — рост требований

11. VPN / WAF / Postgres / внешний аудит — по решению заказчика.
---

## Сравнение: ВМ в облаке vs локально у заказчика

| | Облачная ВМ (публичный IP) | Локально / LAN заказчика |
|--|---------------------------|---------------------------|
| Угроза из интернета | высокая без SG+HTTPS | низкая (если нет проброса портов) |
| Перехват пароля | критичен без TLS | ниже в доверенной LAN, TLS всё равно желателен |
| Бэкапы | нужны вне ВМ обязательно | лента/NAS/регламент IT заказчика |
| Кто отвечает за периметр | вы + облако + заказчик | IT заказчика |

Рекомендация для пилота на ВМ: **этап A обязателен** до расширения круга пользователей. Этапы B–C — до «боевого» режима на той же ВМ.

Связанные документы: `DEPLOYMENT.md`, `docs/PRE_PRODUCTION.md`, `docs/HTTPS_SETUP.md`, `.cursor/rules/releases-and-updates.mdc`.
