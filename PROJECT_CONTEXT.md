## Ветка: main

Текущий фикс (2026-08-26): унифицирован числовой ввод во всех фронтовых `tsx`-формах через `DecimalInput` (разрешён старт с `0`, невалидные символы блокируются с подсказкой). Исправлен кейс «Приёмка», где раньше оставался `input type="number"` без общего поведения.

Пилот: https://vilar-prod.ru
