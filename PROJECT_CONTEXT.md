# PROJECT_CONTEXT

## Ветка: main (v0.2.0)

Слито `feature/roadmap-batch-remaining`:
- actor без Admin-fallback; уникальность серии; Helmet; склады в completeOrder
- QBL блокировка партии + красная подсветка в планировании
- httpOnly cookie-сессии + CORS credentials
- changelog в Админке + простой чат в шапке

Пилот: https://vilar-prod.ru. Env: `/etc/vilar-op.env` (`CORS_ORIGIN`, `TRUST_PROXY`, `COOKIE_SECURE`/`NODE_ENV`).
