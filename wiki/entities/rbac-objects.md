# Entity: RBAC и объекты

Матрица прав роли ↔ objectId. Коллекции документов/регистров ограничены `collectionAccess`.

Избранное и nav pages фильтруются по RBAC.  
Константы: `systemObjects` (BE/FE), `roles.json`.

**UI:** `RolesPage`, `PermissionMatrix`
